本目录用于放置本地化的 ECharts 库文件 echarts.min.js（约 1MB，Apache-2.0）。
页面通过 <script src="lib/echarts.min.js"> 相对路径加载（客户机离线，禁 CDN）。
当前该文件尚未落位（下载文件已就绪但受环境限制未能写入，见节点回传说明）；
在文件放入前，总览页会显示"径向树组件未就绪"提示，其余视图不受影响。
放置完成后直接刷新页面即可。
