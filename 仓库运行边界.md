# 仓库运行边界

这份说明用于判断哪些内容属于公开仓库，哪些内容属于用户下载后持续产生的本地数据。

## 判断原则

- 规则、入口、模板、检查脚本和改造后的源码属于体系本身，应随仓库保留。
- 用户记忆、用户收集的资料、运行状态和生成缓存属于本地数据，不应上传。
- 需要保留目录时，用 `.gitkeep` 或入口文件保留结构；不要把目录中的实际资料一并上传。
- 依赖和构建结果可以由 AI 按安装指引重新安装或生成，不作为仓库必备源码。

## 运行边界

| 模块 | 下载后用途 | 持续产生的本地内容 | 公开仓库保留内容 |
| --- | --- | --- | --- |
| `协作入口/` | AI 识别平台并按任务读取规则 | 无 | 入口、规范、平台适配、检查工具 |
| `角色/` | AI 按任务调用角色和流程 | 项目执行过程中的临时产物不归入此处 | 角色文件、流程、模板、参考资料 |
| `代码开发/` | 提供框架和开发参考 | 使用者自己的项目配置、依赖、构建结果 | 框架源码、模板配置、开发说明 |
| `知识库/收集箱/` | 接收候选和人工投放资料 | `收集区`、`手动收集区`、`暂存区`、`已处理`中的实际内容 | 入口规则、检查脚本、模板、`.gitkeep` |
| `知识库/raw/` | 保存待引用的原始来源 | 用户收集的原文和附件 | 规则和模板，不上传当前来源内容 |
| `知识库/Wiki/` | 提供可复用知识 | 用户或项目专属知识 | 规则、索引和确认后公开的通用知识 |
| `自成长/` | 管理用户画像、经验和记忆 | `自成长/agentbrain/` 中的个人记忆 | 入口、规则和模板 |
| `工具/MCP/` | 提供 MCP 登记、源码和安装指引 | 本地依赖、令牌、运行状态 | `agentbrain` 改造版源码、登记和安装说明 |
| `工具/skills/` | 提供 Skill 登记和安装指引 | 第三方源码、依赖和平台链接 | 自建 Skill、登记和安装说明 |
| `工具/可视化工作台/` | 展示体系结构和流程 | `data-static.js`、更新时生成的 `data.js`、备份和临时文件 | 页面、更新脚本；首次运行自动生成不含个人信息的公开 `data-static.js` |
| `人工维护/` | 提供人工双击执行的维护入口 | 脚本运行日志和临时输出 | `.bat`、说明和维护入口 |

## 公开前重点

- 收集箱目录必须能在新电脑上继续接收内容，但当前收集资料不能随仓库发布。
- `agentbrain` 源码必须保留；个人记忆库不随仓库发布，新用户首次使用时从空白记忆库开始。
- 可视化工作台直接加载运行时生成的 `工具/可视化工作台/data.js`。发布时排除 `data.js` 和 `data-static.js`；新电脑运行 `人工维护/可视化工作台更新数据.bat` 后，脚本会先生成公开模板，再生成 `data.js`。
- 框架模板配置可以保留，但真实密码、令牌、私钥和本机路径必须使用占位符或本地配置。
- `node_modules/`、`.deps/`、`target/`、`dist/`、缓存和备份文件可以重新生成，不属于公开仓库必需内容。
