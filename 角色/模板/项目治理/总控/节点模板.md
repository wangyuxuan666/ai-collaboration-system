---
所属: 角色
必读: 参考
---

# [角色名称] 项目名 - 角色窗口任务

没有更适合的专门角色时，角色名称填写“通用执行者”。

## 1. 任务约定

- 直接调用者（用户或上级任务）：
- 直接调用者实际 ID：
- 结果回传方式：直接向当前用户交付 / 回传直接调用者 / 回传已登记协作对象
- 对接人/协作对象及窗口 ID：<本窗口需要对接的角色及其平台窗口 ID，如"技术经理（窗口 ID: xxx）"；由创建节点的一方填写并同步登记到项目协作目录"窗口目录"，协作对象间凭此直接对接，找不到时按流程/角色文件查窗口目录，仍缺失则反馈创建方补登记>
- 项目协作目录：
- 节点文件路径：<项目根目录>/AI协作/节点/<节点文件>.md
- **文档存放位置**：<本节点产出的文档放哪，如"AI协作/方案/接口文档.md"；由创建节点的一方（总控或上级角色）按代码开发专业领域默认文件结构填写，下窗口按此存放与读取；无特殊说明按默认结构执行> 
- 当前窗口 ID（创建新窗口后填写平台实际对话或任务 ID）：
- 任务摘要：
- 当前专业领域：<角色名 + 方向，如"前端开发·UI 方向"；**填写则按该领域加载对应专业领域文件，不填写则默认普通角色（不加载任何专业领域）**>
- 节点执行流程：
- 推荐执行等级：轻量 / 标准 / 高级
- 推荐理由：
- 用户确认的执行等级与模型：
- 升级条件：
- 执行方式：直接执行 / 轻量 ReAct / ReAct
- 尝试预算：根据单次成本、风险、恢复难度和验证成本填写；不设统一固定次数。实际消耗明显超出预算，或连续尝试没有新证据时，立即暂停并回传直接调用者，由其决定调整范围、执行方式或升级用户
- 提前停止依据：继续尝试已无合理价值时如何判断
- 验收等级与模型：
- 目标：
- 范围：
- 输入：
- 已确认决定与不得静默改变的事项：
- 项目上下文及正式文档入口：
- 相关 Git 基线：
- 运行、测试或预览入口：
- 依赖：
- 交付物：
- 验收标准：
- 首次交付回传约定：填写直接调用者和回传方式
- 验收通过后的上行汇总对象：<默认回传直接调用者，不需另行询问用户>
- 验证方式：
- 权限与风险边界：
- 变更前基线或备份：
- 停止条件：
- 回滚触发条件与方式：
- 检查点记录位置：
- 暂停与恢复条件：

## 2. 准备检查

- 检查结论：可以执行 / 等待补充 / 阻塞
- 已确认的目标、范围与输入：
- 依赖与权限检查：
- 执行等级复核：适合 / 建议升级 / 建议降低
- 执行等级复核依据：
- 验收和验证可执行性：
- 基线、备份与恢复检查：适用且充分 / 不适用 / 存在缺口
- 检查点与恢复准备：适用且充分 / 不适用 / 存在缺口
- 需要用户处理的问题：
- 对任务包的独立核对结论：一致 / 存在偏差 / 信息不足
- 直接调用者与回传方式检查：完整 / 缺失，缺失时先询问
- 尝试预算检查：与成本、风险、恢复和验证难度相称 / 需要调整

## 3. 节点结果回报

- 角色窗口状态：待执行 / 准备检查 / 执行中 / 已暂停 / 等待反馈 / 待验收 / 已完成 / 阻塞 / 已取消
- 实际完成内容：
- 实际交付物：
- 验证结果与证据：
- 完成前自检：目标与范围 / 交付物 / 验收证据 / 权限边界 / 风险 / 相关回归
- 当前检查点：已验证事实、完成交付、未决问题、下一恢复动作和恢复条件
- 恢复指令：`恢复项目“<项目名称>”的节点“<节点名称>”：读取 <节点文件路径>，以“<角色名称>”继续执行。`
- 与原计划的偏差：
- 未解决问题或剩余风险：
- 后续节点交接信息：
- 是否需要调用者决定：
- 结果回传记录：回传对象、实际动作、时间与状态；无法联系时记录限制和待转交方式
- 自成长记录：仅在自成长实质影响关键判断，或产生、修正自成长记忆时填写编号与确认结果
- 调用者验收结论与上行汇总记录：
- 阶段汇总归属：由哪个阶段负责人吸收，或本节点即为阶段汇总

## 3.1 窗口接续记录

| 窗口 ID | 接续时间 | 接续原因 | 接续前检查结论 | 状态 |
|---|---|---|---|---|

## 4. 调用者验收

- 调用者验收结论：通过 / 返工 / 阻塞 / 取消
- 验收依据：
- 未满足项：
- 后续动作：

> “部分完成”不是节点终态。执行者未满足验收标准时继续循环；自检通过后进入“待验收”，直接调用者验收通过后才进入“已完成”。无法继续时进入“阻塞”。
