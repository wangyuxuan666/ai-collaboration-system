---
所属: 自成长
必读: 按需
---

# 经验检索名单

## 默认规则

**每个有效动作前，先对照经验索引（Index.md）判断该动作是否有相关经验；有 → 执行记忆检索；没有 → 跳过，不空查。**

检索的目的不是判断"要不要查"，而是看当前动作有没有可复用的经验教训；索引先行是为了省 token，避免对没有经验覆盖的动作空查。有经验覆盖却不检索就直接执行，等于放弃已有经验，会重复犯错。

## 免查名单

仅以下场景跳过检索：

- 仅进行角色设定、没有具体任务时
