---
所属: 自成长
必读: 必读
---

# 用户画像入口

## 概念

- **用户画像（Profile）**：用户稳定事实与全局偏好的集合，由 agentbrain 保存；读取用 `memory_profile()`，修改按[维护规则](./维护规则.md)。

## 读取

- 每个窗口调用一次 `memory_profile()`（不传参），获取画像全文；后续不重复调用，也不直接读取画像文件。
- 新增、修改、删除或推测全局用户信息与偏好时，读取[维护规则](./维护规则.md)；正常读取画像时不读。
